# Mobile App Configuration
# ⚠️ Note: React Native doesn't read .env files without additional libraries
# 
# REQUIRED: Update mobile/src/config.ts directly with these values:
#
# const MAC_IP = 'YOUR_MAC_IP_HERE';                    // Your Mac's local IP
# const RELAY_SERVER_URL = 'http://YOUR_MAC_IP_HERE:3000'; // Relay server URL
# const DEBUG_MODE = 'true';
#
# Find your Mac's IP:
#   System Settings → Network → Wi-Fi → Details
#   or run: ipconfig getifaddr en0

# Example values (update config.ts with your actual values):
MAC_IP=192.168.178.72
RELAY_SERVER_URL=http://192.168.178.72:3000
DEBUG_MODE=true

# For iOS Simulator (shares host network):
# MAC_IP=localhost
# RELAY_SERVER_URL=http://localhost:3000

# For Physical Device (same WiFi as Mac):
# MAC_IP=192.168.x.x  (your Mac's actual IP)
# RELAY_SERVER_URL=http://192.168.x.x:3000
